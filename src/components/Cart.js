import React from 'react';
import {Grid} from "@mui/material";

const Cart = () => {
    return (
        <Grid>
            <Grid>
                cart items
            </Grid>
            <Grid>
                right panel
            </Grid>
        </Grid>
    );
};

export default Cart;